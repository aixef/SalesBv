({
    init: function(component,event,helper){
        helper.helperInit(component,event,helper);
    },

    selectProduct: function(component,event,helper){
        helper.helperSelectProduct(component,event,helper);
    }
})
